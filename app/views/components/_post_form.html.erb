<%= form_for(@new_post) do |f| %>
  <center>
      <div class="field">
        <%= f.text_field :content, autofocus: true %>
      </div>
    
      <div class="actions">
        <%= f.submit "App Post" %>
      </div>
  </center>
<% end %>

